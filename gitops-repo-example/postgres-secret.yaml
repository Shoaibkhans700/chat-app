# Example-only credentials for a local/dev cluster. Never reuse these
# values anywhere real - see k8s/secret.yaml for the pattern used by the
# actual (RDS-backed) deployment, including why Secrets alone aren't
# enough for production.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: chat-app
type: Opaque
data:
  POSTGRES_USER: Y2hhdGFwcA==            # chatapp
  POSTGRES_PASSWORD: Y2hhdGFwcC1sb2NhbC1vbmx5   # chatapp-local-only
  POSTGRES_DB: Y2hhdGFwcA==              # chatapp
