# Copy to .env for local (non-docker-compose) runs. Never commit the real .env.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chatapp
DB_USERNAME=chatapp
DB_PASSWORD=chatapp
JWT_SECRET_KEY=dev-only-secret-change-me
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
CORS_ORIGINS=http://localhost:5173
